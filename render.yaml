services:
  - type: web
    name: krishna-medical-billing
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir KrishnaMedicalBilling app:app
    envVars:
      - key: FLASK_ENV
        value: production
